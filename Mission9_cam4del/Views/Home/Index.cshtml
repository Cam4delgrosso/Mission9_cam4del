@{
    ViewData["Title"] = "Home Page";
}
@model BooksViewModel


@*For loop to display all books currently in the database*@
@foreach(Book b in Model.Books)
    {
        <partial name="BookInfo" model="b"/>
    }
